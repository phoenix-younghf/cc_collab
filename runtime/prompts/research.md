Research the task and return a structured result for the local collaboration runtime.

Return exactly one JSON object that matches the provided schema.
Do not wrap the JSON in markdown fences.
Do not add commentary before or after the JSON object.
Do not inspect the workspace or use tools unless the task explicitly requires it.
If the request can be satisfied from the provided task description alone, return the minimal valid structured result immediately.
Use empty arrays when there is nothing to report.
